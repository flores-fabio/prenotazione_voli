// Ambiente di produzione per l'app Angular
// Modificare l'apiUrl con l'URL pubblico del backend quando si distribuisce
export const environment = {
  production: true,
  apiUrl: 'http://localhost:5000/api'
};
